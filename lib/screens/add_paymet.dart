import 'package:flutter/material.dart';

class AddPaymetScreen extends StatelessWidget {
   
  const AddPaymetScreen({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:const Text("Add Payment"),),
      body: Center(
         child: Text("data"),
      ));
    
  }
}